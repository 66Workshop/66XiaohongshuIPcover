# 66Workshop｜ChatGPT Cover Identity-Lock Skill V0.1

## 1. 任务目标

把用户上传的 **封面标题 + 66Workshop 授权真人/IP 图 + 车型实拍图 + 配件实拍图 + 字体/版式参考图**，转成 3:4 小红书/抖音封面。

核心目标不是“生成一个像辉哥的人”，而是：

1. **优先保留本人身份**。
2. **优先保留真实车型与配件证据**。
3. 在身份安全前提下适配构图、身体姿态、光影、标题与场景。
4. 任何“像，但不是本人”的脸都判定失败。

> P0 固定优先级：**人物身份保真 > 车型身份保真 > 配件/证据保真 > 标题正确 > 版式冲击 > 装饰效果。**

---

## 2. 产品边界｜必须先接受

- ChatGPT 推理模型负责理解素材、角色绑定、路线选择、生成/编辑指令、QA 与回退。
- 实际图片由 ChatGPT 图像生成/编辑能力完成。
- “锁定人脸”“负面词不要换脸”不是原生 ChatGPT 暴露出来的硬身份参数，不能保证生成模型绝不重绘人脸。
- **因此本 Skill 不把提示词当成身份锁；把“少生成本人、更多沿用真实本人素材”作为第一原则。**
- V0.1 不宣称原生 ChatGPT 可以在任意大幅姿态变化下实现数学意义上的 100% 人脸一致。
- Route A 的 `SOURCE_PRESERVE_COMPOSITE` 表示“最大化源人物保留”，**不表示图像模型具有真正不可写的像素锁定区**。

只要目标姿态需要大幅重做头部/脸部，必须降低姿态要求、换更接近的授权人物图，或明确判定 `identity-safe output not achieved`。

---

## 3. 上传素材自动角色绑定

在调用创建图片前，先把每张图绑定到唯一主角色：

- `PERSON_IDENTITY_PRIMARY`：本人身份主锚点
- `PERSON_IDENTITY_SECONDARY`：本人身份补充图，最多 2 张
- `PERSON_POSE_SOURCE`：姿态来源
- `VEHICLE_EVIDENCE`：真实车型证据
- `PRODUCT_EVIDENCE`：卡钳/碟盘/轮毂/避震等真实配件证据
- `FONT_STYLE_REFERENCE`：字体视觉风格参考
- `LAYOUT_REFERENCE`：封面版式参考
- `BACKGROUND_REFERENCE`：车间/环境参考

### 禁止

- 把所有上传图无差别丢给生图模型。
- 把字体图当人物风格参考。
- 把成功案例里其他人物的脸当身份参考。
- 用 4 张以上不同角度人脸图“堆参考”赌身份稳定。

默认：**1 张主身份锚点 + 0–2 张补充身份图。**

---

## 4. 主身份锚点选择

按以下优先级自动选择：

1. 与目标头部朝向最接近。
2. 人脸分辨率最高、最清晰。
3. 表情自然、无严重运动模糊。
4. 无墨镜、手、头发、强阴影大面积遮挡。
5. 身体姿态与目标场景最接近。

绿幕/透明 PNG 真人素材若能满足构图，优先使用它，不让模型从零“重新画一个本人”。

---

## 5. 三条身份路线｜必须先路由再出图

### A. `SOURCE_PRESERVE_COMPOSITE`｜默认首选

适用：现有人物图姿态已经基本可用。

目标：**把授权真人源当成主体锚点，画面围绕真人适配，尽量减少人物重构。**

允许：

- 抠绿幕/透明化意图
- 移动、缩放、裁切
- 轻微整体旋转
- 调整人物前后层级
- 轻微亮度、色温、对比度匹配
- 接触阴影、轮廓光、环境反射
- 调整背景、车辆、配件、标题来适配真人源

禁止：

- 主动要求重画五官
- 换脸
- 美颜、瘦脸、磨皮
- 改眼睛、鼻子、嘴、下颌线、发际线
- 为了“更酷”重新设计脸
- 把真人转成插画脸/3D脸/明星脸

> 注意：在 ChatGPT 原生生成式编辑里，A 路线仍不能保证源脸像素逐像素完全不变；它只是当前原生工作流里生成风险最低的路线。

**只要 A 能满足任务，就不要升级到 B/C。**

### B. `SOURCE_EDIT_MINIMAL`｜次选

适用：需要中等程度身体姿态变化，但已有授权真人图可以作为编辑底图。

原则：

- 从 1 张主身份源图开始，不从空白画布开始。
- 把人脸/头部视为身份保护区。
- 只修改满足姿态所需的最小区域。
- 保持眼型、眼距、眉形、鼻型、嘴型、脸型、下巴、发际线、年龄感、肤色不变。
- 优先改手臂、肩膀、躯干、裁切、前后关系，不主动改变头部方向。

如果 B 出图后脸明显漂移，立即 FAIL，回退 A 或换更接近目标姿态的真人源图。

### C. `GENERATIVE_IDENTITY_REFERENCE`｜最后手段

适用：没有任何真人源图能满足目标姿态。

规则：

- 只允许作为候选，不自动通过。
- 使用 1 张主身份锚点，最多 2 张补充身份图。
- 补充图只能确认身份，不能同时提供多个冲突的姿态/光线/年龄目标。
- 禁止“参考某人风格”“长得像本人”“inspired by”这类弱约束。
- 明确要求：**same authorized person, not a lookalike, not a reinterpretation**。

连续 2 次身份失败：停止 C，不再盲抽，回退 B/A 或更换真人姿态源。

---

## 6. 姿态风险分级

### `POSE_LOW`

- 裁切、位置、大小变化
- 轻微身体前倾/后仰视觉感
- 手部局部露出变化
- 头部方向基本不变

优先 A，可用 B。

### `POSE_MEDIUM`

- 肩膀/手臂/躯干明显变化
- 站姿/坐姿附近的中等变化
- 脸仍保持接近原始角度

优先 B；C 必须严格 QA。

### `POSE_HIGH`

- 正脸变大侧脸
- 大幅低头/仰头
- 极端透视
- 戏剧性表情变化
- 全身大幅重构并伴随头部方向变化

**V0.1 不承诺 POSE_HIGH 的稳定锁脸。**

优先换接近目标动作的授权真人图，而不是继续让模型硬生成人脸。

---

## 7. ChatGPT 自动执行状态机

### STATE 0｜`PREFLIGHT`

自动完成：

- 识别标题
- 识别人物/车/配件/字体图
- 给每张图绑定角色
- 选主身份锚点
- 判断 `POSE_LOW / MEDIUM / HIGH`
- 判断输入是否足够

缺少关键身份源时，不直接生成假脸。

### STATE 1｜`ROUTE_SELECT`

固定顺序：

`SOURCE_PRESERVE_COMPOSITE` → `SOURCE_EDIT_MINIMAL` → `GENERATIVE_IDENTITY_REFERENCE`

永远选择满足任务的最低生成风险路线。

### STATE 2｜`COMPOSITION_PLAN`

调用创建图片前，内部先固定：

- 人物位置/占比
- 车辆位置/占比
- 配件证据位置
- 标题区
- 视觉焦点
- 人脸与车型不可遮挡区

不要先随机出图再解释。

### STATE 3｜`IMAGE_CREATE_OR_EDIT`

必须把上传图按角色明确写入指令。

身份主指令：

> `PERSON_IDENTITY_PRIMARY` 是本封面唯一主人物身份源。保持同一位授权真人，不生成相似脸，不重新设计五官。最大化沿用源图的脸部/头部身份，只为构图所必需的区域做最小修改。

同时明确：

- 哪张是车型实图
- 哪张是配件实图
- 哪张只提供字体风格
- 哪张只提供版式参考

### STATE 4｜`QA_GATE`

出图后必须对照源图检查：

1. 人脸身份
2. 车型
3. 配件
4. 标题文字
5. 版式
6. 66Workshop 视觉语言

### STATE 5｜`REPAIR_OR_FALLBACK`

只修失败区域：

- 文字错 → 只修文字
- 背景差 → 只修背景
- 车漂 → 修车辆区域/换车锚点
- 配件错 → 修配件区域/降低露出
- **脸错 → 不用“再修一下脸”继续赌；优先降姿态、减参考、换源图、回退路线。**

### STATE 6｜`RECORD`

记录：

- identity route
- pose risk
- 主身份资产
- 车辆/配件证据
- 重试次数
- identity score
- 最终人工 verdict
- 失败原因

---

## 8. Identity QA｜人物一票否决

对比 `PERSON_IDENTITY_PRIMARY`：

- [ ] 脸型/整体比例一致
- [ ] 眼型、眼距一致
- [ ] 眉形与眉眼关系一致
- [ ] 鼻型一致
- [ ] 嘴型/唇部结构一致
- [ ] 下巴/下颌比例一致
- [ ] 发际线/发型身份特征一致
- [ ] 年龄感一致
- [ ] 肤色与本人特征一致
- [ ] 无明显美颜换脸感
- [ ] 没有变成另一个“更帅的陌生人”

### 身份等级

- `IDENTITY_5`：源脸高度保留，封面使用尺度下近似不可区分
- `IDENTITY_4`：明确是本人，仅有轻微非身份渲染差异
- `IDENTITY_3`：大概率是本人但已有明显漂移 → FAIL
- `IDENTITY_2`：相似脸 → FAIL
- `IDENTITY_1`：其他人 → FAIL

生产通过标准：**IDENTITY_4 或 IDENTITY_5 + 用户本人/授权人工确认。**

---

## 9. 车型与配件 Evidence Lock

### 车型

- 有真实车图必须优先真实车图。
- 年款、灯组、车身比例、保险杠、轮毂、姿态、徽标不自由设计。
- 无法稳定保真时，宁愿减少露出，也不要生成假车型细节。

### 配件

卡钳、碟盘、轮毂、避震等属于证据资产：

- Logo 不乱写
- 型号不乱写
- 卡钳结构不乱改
- 碟盘结构不乱改
- 螺丝位/安装结构不虚构
- 轮毂辐条不随意变化

信息不足时隐藏细节或标 UNKNOWN，不造事实。

---

## 10. 字体与标题

- 字体截图 = `FONT_STYLE_REFERENCE`，不是可调用字体文件。
- 不得声称“用了原字体文件”，除非工作流确实提供并支持该字体。
- 主标题 7–16 中文字优先，两行优先。
- 一张封面不超过 3 个视觉信息层级。
- 标题出图后逐字检查。
- 文字失败优先局部修字，不重生整张图导致人脸再次漂移。

标题逻辑：

- 事实/对象/背景：黑/白
- 冲突/结论/质疑：红/黄重点
- 禁止“震惊、颠覆认知、必看”等空营销词

---

## 11. 66Workshop 已验证版式

### P01｜DATA_CONFLICT

适用：减重、实测、价格、尺寸、制动距离。

- 上 30–35%：强标题/数字冲突
- 中下：真人 IP
- 另一侧：整车
- 前景：秤、卡钳、盘、轮毂等证据

### P02｜BEFORE_AFTER

适用：清洗、翻新、维修、拆解、避坑。

- 真人 30–40%
- 右侧主证据 40–50%
- 1–2 张前后对比
- 标题上方

### P03｜VEHICLE_DECISION

适用：为什么改、怎么选、先看什么。

- 车型 35–50%
- 真人 25–35%
- 1–2 个配件证据
- 标题上方

### P04｜PRODUCT_VALUE

适用：卡钳/避震/轮毂值不值。

- 产品/车型主视觉
- 真人负责“判断感”
- 2–3 个证据细节
- 禁止做成廉价电商详情页

---

## 12. 视觉系统

- 比例：3:4
- 优先真实车间/摄影棚/深灰工业空间
- 主色：黑 / 白 / 红，黄只少量强调
- 人物应与车、配件有真实空间关系
- 禁止人物漂浮、比例失真、配件悬空
- 禁止强 CG、夜店感、九宫格参数 UI、小标签堆满画面
- 66Workshop 标识小而克制

---

## 13. 自动失败回退

### 第一次脸漂

1. 从多张身份参考降到 1 张主锚点。
2. 降低姿态变化。
3. 换更接近目标角度的真人图。
4. C → B，或 B → A。

### 第二次同路线脸漂

停止同一路线。

不得用“再生成 5 张总有一张像”的办法解决。

### 停止条件

如果真人素材无法支持目标姿态，同时 B/C 仍未达到 IDENTITY_4：

> **Identity-safe output not achieved. 不接受相似脸作为最终封面。**

---

## 14. Benchmark

V0.1 至少跑 20 个真实封面任务：

- human-approved identity pass rate ≥ 95%
- title correctness ≥ 98%
- vehicle/evidence correctness ≥ 95%
- first-pass usable rate ≥ 60%
- 不允许任何 `IDENTITY_1/2/3` 被标记为通过

同时统计：

- A/B/C 路线占比
- LOW/MEDIUM/HIGH 姿态失败率
- 平均返工次数
- 最大失败原因

达不到目标时，先改人物姿态资产库、路由与流程，不先加复杂模型。

---

## 15. 调用入口

用户完成上传后，可直接说：

> 按 `66Workshop ChatGPT Cover Identity-Lock Skill V0.1` 执行。标题是：XXX。用我上传的 IP 图锁定本人身份，用车型与配件实物图做证据，字体图只作为字体风格参考。做 3:4 封面，先自动选择最安全的身份路线，再创建图片；出图后必须做人脸、车型、配件和文字 QA，不是本人就直接判失败并按回退规则重做。

详细协议：

- `docs/INPUT_SPEC.md`
- `docs/WORKFLOW.md`
- `docs/QA_CHECKLIST.md`
- `docs/FAILURE_PLAYBOOK.md`
- `docs/TEST_PLAN.md`
- `prompts/CHATGPT_IDENTITY_LOCK_PROTOCOL.md`
